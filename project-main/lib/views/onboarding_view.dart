import 'package:flutter/material.dart';

class OnboardingView extends StatelessWidget {
  const OnboardingView({super.key});

  @override
  Widget build(BuildContext context) {
    // we will use pageview widget
    // onboarding view with logo of the app and title and description of the app
    // onboarding vwith logo of the app and title and description of the app
    // onboarding with logo of the app and title and description of the app
    // direct to login view  or home view based on user data
    return Container();
  }
}
