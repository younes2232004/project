import 'package:flutter/material.dart';

class HomeView extends StatelessWidget {
  const HomeView({super.key});

  @override
  Widget build(BuildContext context) {
    // depends on project requirement we can show the home view
    // we will use layoutout widgets like column, row, stack, gridview, listview, etc
    // we will use the app bar, bottom navigation bar, drawer, etc
    // we will use the card, listtile, etc
    return Container();
  }
}
